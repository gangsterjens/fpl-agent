version: "3.9"
services:
  app:
    build: .
    image: fpl-agent:dev
    volumes:
      - .:/app
      - hf_cache:/app/.cache/huggingface
    environment:
      HF_HOME: /app/.cache/huggingface
    command: python run.py

volumes:
  hf_cache:
